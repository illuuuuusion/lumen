# Discord bot token (never commit the real value)
DISCORD_TOKEN=

# Shared secret for the internal event API (phase 8)
LUMEN_INTERNAL_API_SECRET=

# Path to the non-sensitive config file (phase 1)
LUMEN_CONFIG_PATH=./config.yml

# Path to the SQLite database file (phase 2)
LUMEN_DATABASE_PATH=./data/lumen.db
